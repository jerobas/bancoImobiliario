name: Push-to-EC2
on:
  push:
    branches:
      - master
jobs:
  deploy:
    name: Deploy to EC2
    runs-on: ubuntu-latest

    steps:
    - name: Checkout the files
      uses: actions/checkout@v3
      
    - name: Copy files with SSH
      uses: easingthemes/ssh-deploy@main
      env:
        SSH_PRIVATE_KEY: ${{secrets.EC2_SSH_KEY2}}
        ARGS: "-rltgoDzvO"
        SOURCE: "./"
        REMOTE_HOST: ${{secrets.REMOTE_HOST2}}
        REMOTE_USER: ${{secrets.REMOTE_USER2}}
        TARGET: ${{secrets.TARGET_DIR2}}
        EXCLUDE: "/dist/, /node_modules/, **.env, rebuild_app.sh, watcher.sh"
        
    - name: create pem
      run: echo "${{secrets.EC2_SSH_KEY2}}" > key.pem
      
    - name: chmod
      run: sudo chmod 400 key.pem
      
    - name: Run API
      run: ssh -i key.pem ${{secrets.REMOTE_USER2}}@${{secrets.REMOTE_HOST2}} "sudo pkill -9 node; yarn; yarn build &"
